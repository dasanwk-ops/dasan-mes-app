# 실험로 기능 운영 반영 — 2026-09-23

사용자가 테스트 버전을 확인하고 main 반영을 승인했습니다.
기준 main: `3d425e69ab0b653c4dbfe080d59e1a69304ed69f`.

## 반영 범위

아래 기능 커밋만 최신 main에 적용했습니다. 테스트 브랜치 전체를 병합하지 않았습니다.

- `ff7f062fedf972b4330a4bd507a0ada296dfee93`: 층 구분 없는 실험로.
- `696913efc63d82de0197e28fc4136e1c0f9bd089`: 합계 10개, 실험용/시편 선택, 미측정 이관.
- `f9e2c1443a41eca3ca997a6dfd68d6594110bdf7`: 시편 포함 분말 계산을 일반 생산과 일치.
- `74af770084a309abbce33607defd7fa79b652021`: 여러 로트 동시 적재, 실제 필요한 통만 표시, 실험로 위치 입력 생략, 라벨 장수, TEST 추적.

운영 Firebase 설정·인증·PIN·기본 진입 화면·Google Sheets 연동은 유지합니다.
샘플 데이터, 자동 관리자 로그인, 샘플 초기화 버튼, sessionStorage 어댑터 및 네트워크 차단 설정은 반영하지 않았습니다.

## 기존 생산 데이터와 공정

- DB 일괄 수정·삭제·초기화·마이그레이션을 실행하지 않습니다. 운영 DB에 테스트 데이터를 생성하지 않습니다.
- 기존 로트의 실험용 필드가 없으면 일반 생산으로 처리합니다. 기존 로트를 실험용으로 일괄 전환하지 않습니다.
- 일반 분말 계산(제품 분말 × 1.01 + 0.200kg), 1·2호기 층 구분과 시편 위치/면적 검증을 유지합니다.
- 기존 로트 수량·공정·과거 이력과 가동 중 전기로·측정대·대기 회차를 그대로 읽어 이어갑니다. 실험로는 별도 `lab` ID를 사용합니다.
- 관리자 수량 정정, 공정 롤백, 기존 수동 삭제 기능과 권한을 유지합니다.
- 공통 화면에서는 불필요한 15kg 배합표가 숨겨지고 라벨 장수/미리보기가 추가됩니다. 라벨 출력 후 수량·LOT·수축률 등이 바뀌면 재출력이 필요합니다.
- 기존 버전에서 정상적으로 라벨을 출력한 일반 제품은 새 실험용 필드가 없어도 포장 완료할 수 있습니다.

## 검증

- `node --test tools/test-heat-completion.cjs`: 26개 통과. 실제 완료 핸들러의 이력·수량·다른 설비 보존, 충돌 차단, 실험로 합계 용량, 일반 분말 계산 포함.
- `npm run build`: 기존 3개 빌드 변환 모두 적용 후 성공. 실제 운영 Firebase SDK로 번들 생성.
- 변환된 운영 소스를 실제 PIN 입력 화면부터 실행하는 React DOM 통합 검증 25개 통과. Firebase와 시트만 격리된 임시 모의 연결로 교체했으며 운영 DB/프린터로 요청하지 않았습니다.
- 발주부터 분말 차감·성형·다품종 실험로·측정/미측정·포장·출고·TEST 추적 확인.
- 모든 공정 단계의 기존 형식 로트를 불러올 때 수량·공정·이력이 재작성되지 않는지, 가동 중 전기로/측정대/대기 회차가 유지되는지 확인.
- 배포 전 출력 이력만 있는 일반 제품의 재출력 없는 포장 완료 확인.
- 운영 접속 설정과 권한 코드가 기준 main과 동일하고 테스트 연결 코드가 없는지 확인.

실제 프린터와 외부 인쇄 템플릿의 인쇄 결과는 확인하지 않았습니다. 일반 라벨의 기존 제품 식별자·GTIN·출력 수량 필드는 유지합니다. 미측정 실험품은 수축률/확대율에 `미측정`을 전달합니다.

## 되돌리기 참고

문제가 생기면 운영 반영 PR을 되돌리는 새 커밋으로 코드를 복구합니다. 데이터 삭제나 이력 복구 스크립트를 실행하지 않습니다. 이미 실험로에 작업이 배정된 뒤에는 이전 화면이 실험로를 표시하지 않으므로, 해당 작업 상태를 먼저 확인하고 코드 롤백 여부를 판단해야 합니다.
